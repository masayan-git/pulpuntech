<%= form_with(model: masters_profile, local: true) do |form| %>
  <% if masters_profile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(masters_profile.errors.count, "error") %> prohibited this masters_profile from being saved:</h2>

      <ul>
        <% masters_profile.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
